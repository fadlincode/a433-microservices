# SHIPPING SERVICE : 3001
# CONSUMER
# ------------------------------
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  labels:
    app: shipping-service
    tier: consumer
spec:
  selector:
    app: shipping-service
    tier: consumer
  ports:
    port: 3001